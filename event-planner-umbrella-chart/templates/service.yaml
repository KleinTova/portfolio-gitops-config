apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-planner-umbrella-chart.fullname" . }}
  labels:
    {{- include "event-planner-umbrella-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-planner-umbrella-chart.selectorLabels" . | nindent 4 }}
