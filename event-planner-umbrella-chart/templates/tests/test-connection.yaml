apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-planner-umbrella-chart.fullname" . }}-test-connection"
  labels:
    {{- include "event-planner-umbrella-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-planner-umbrella-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
